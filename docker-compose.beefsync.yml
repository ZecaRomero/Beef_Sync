# Docker Compose para Beef Sync
# Build na imagem - sem npm ci/build no startup

version: '3.8'

services:
  beefsync-app:
    build: .
    container_name: beefsync-app
    restart: unless-stopped
    ports:
      - "3020:3020"
    environment:
      - NODE_ENV=production
